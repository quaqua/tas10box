<% if flash[:notice] %>
  $('#tab_<%= @doc.id %> .tab-close').click();
  $('[data-id=<%= @doc.id %>]').remove();
  $('#<%= @doc.id %>').remove();
<% end %>
tas10.notify( '<%= escape_javascript( flash[:error] || flash[:notice] ) %>' );