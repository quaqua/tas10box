!!! 5
%html
  %head

    = javascript_include_tag :tas10box
    = javascript_include_tag :defaults
    = stylesheet_link_tag :tas10box
    = stylesheet_link_tag :defaults
    = csrf_meta_tag

    %title= "#{Tas10box.defaults[:site][:name]}'s tas10box"
    = yield(:head)

  %body
    = yield

    %script{ :type => 'text/javascript' }
      I18n.defaultLocale = "#{ I18n.default_locale }";
      I18n.locale = "#{ I18n.locale }";
      I18n.fallbacks = true;